apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: gateway-conformance-default
  namespace: gateway-conformance-infra
spec:
  gatewayClassName: tigera-gateway-class
  listeners:
  - name: http
    protocol: HTTP
    port: 80
    allowedRoutes:
      namespaces:
        from: All
