# Generated by confd
filter calico_export_to_bgp_peers {
  # filter code terminates when it calls `accept;` or `reject;`, call apply_communities() before calico_aggr()
  apply_communities();
  calico_aggr();

  # Export static routes.
  if ( net ~ fd00:96::/112 ) then { accept; }
  if ( net ~ fd00:96::28/128 ) then { accept; }

  reject;
}

filter calico_kernel_programming {

  # Don't program static routes into kernel.
  if ( net ~ fd00:96::/112 ) then { reject; }
  accept;
}
