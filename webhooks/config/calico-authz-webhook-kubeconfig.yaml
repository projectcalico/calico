# Kubeconfig for the Calico tier-based RBAC authorization webhook.
#
# This file tells the kube-apiserver how to reach the Calico webhook's /authz
# endpoint. It is referenced from the AuthorizationConfiguration file
# (authorization-configuration.yaml) via the connectionInfo.kubeConfigFile field.
#
# Before use, update the following fields:
#   - server:                The webhook service URL (shown below for in-cluster).
#   - certificate-authority: Path to the CA cert that signed the webhook's TLS cert.
apiVersion: v1
kind: Config
clusters:
  - name: calico-authz-webhook
    cluster:
      server: https://calico-webhooks.calico-system.svc:6443/authz
      certificate-authority: /etc/kubernetes/pki/calico-authz-webhook-ca.crt
users:
  - name: kube-apiserver
    user: {}
contexts:
  - name: default
    context:
      cluster: calico-authz-webhook
      user: kube-apiserver
current-context: default
