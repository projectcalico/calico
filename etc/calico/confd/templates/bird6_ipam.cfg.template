# Generated by confd
filter calico_pools {
  calico_aggr();
{{range ls "/pool"}}{{$data := json (getv (printf "/pool/%s" .))}}
  if ( net ~ {{$data.cidr}} ) then {
    accept;
  }
{{end}}
  reject;
}
