# Generated by confd
filter calico_export_to_bgp_peers {
  calico_aggr();
{{range ls "/pool"}}{{$data := json (getv (printf "/pool/%s" .))}}
  if ( net ~ {{$data.cidr}} ) then {
    accept;
  }
{{end}}
  reject;
}
