- name: Crypto
  run:
    when: "${CHANGE_IN(crypto)}"
  dependencies:
    - Prerequisites
  task:
    prologue:
      commands:
        - cd crypto
    jobs:
      - name: "Crypto: CI"
        commands:
          - ../.semaphore/run-and-monitor ci.log make ci
