- name: Check images availability
  run:
    when: "${FORCE_RUN} or change_in(['/charts/', '/manifests/'], {pipeline_file: 'ignore'})"
  dependencies: []
  task:
    jobs:
      - name: Check images availability
        commands:
          - make check-images-availability
