- name: Prerequisites
  dependencies: []
  task:
    secrets:
      - name: gcp-tigera-dev-service-account
    jobs:
      - name: Pre-flight checks
        commands:
          - make ci-preflight-checks
