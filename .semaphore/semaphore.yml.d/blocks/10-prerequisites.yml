- name: Prerequisites
  dependencies: []
  task:
    jobs:
      - name: Pre-flight checks (batch A)
        commands:
          # Set some images pulling in the background, which we know will be used later.
          - docker pull tmknom/prettier &
          - docker pull cytopia/yamllint &
          - make ci-preflight-checks-a
      - name: Pre-flight checks (batch B)
        commands:
          - make ci-preflight-checks-b
