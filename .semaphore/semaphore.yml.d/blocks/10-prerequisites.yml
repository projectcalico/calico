- name: Prerequisites
  run:
    when: "${FORCE_RUN} or change_in(['/'], {pipeline_file: 'track'})"
  dependencies: []
  task:
    agent:
      machine:
        type: f1-standard-2
        os_image: ubuntu2204
    jobs:
      - name: Pre-flight checks
        commands:
          - make ci-preflight-checks
