- name: libcalico-go
  run:
    when: "${CHANGE_IN(libcalico-go)}"
  dependencies:
    - Prerequisites
  task:
    prologue:
      commands:
        - cd libcalico-go
    jobs:
      - name: "libcalico-go: CI"
        commands:
          - ../.semaphore/run-and-monitor make-ci.log make ci
