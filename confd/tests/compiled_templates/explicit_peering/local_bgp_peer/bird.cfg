# Generated by confd
# Single unified BIRD3 daemon config serving both IPv4 and IPv6.
# Include order matters: BIRD requires symbols (functions/filters) to be
# defined before they are referenced. The dependency chain is:
#   bird_aggr.cfg  (defines calico_aggr_vN, static routes)
#   bird_ipam.cfg  (defines apply_communities_vN, calico_export_to_bgp_peers_vN,
#                   calico_kernel_programming_vN -- depends on calico_aggr_vN)
# Then the kernel/BGP protocols below depend on the *_ipam.cfg symbols.
include "bird_aggr.cfg";
include "bird_ipam.cfg";
router id 10.192.0.2;
# Pin timestamp format to single-token for parser compatibility
timeformat protocol "%F";
timeformat route "%T";
# Watch interface up/down events.
protocol device {
  debug { states };
  scan time 2;    # Scan interfaces every 2 seconds
}
protocol direct {
  debug { states };
  ipv4;
  ipv6;
  interface -"cali*", -"kube-ipvs*", "*"; # Exclude cali* and kube-ipvs* but
                                          # include everything else.  In
                                          # IPVS-mode, kube-proxy creates a
                                          # kube-ipvs0 interface. We exclude
                                          # kube-ipvs0 because this interface
                                          # gets an address for every in use
                                          # cluster IP. We use static routes
                                          # for when we legitimately want to
                                          # export cluster IPs.
}
# Configure synchronization between routing tables and kernel (IPv4).
protocol kernel {
  ipv4 {
    export filter calico_kernel_programming_v4; # Default is export none
    import all;
  };
  learn;             # Learn all alien routes from the kernel
  persist;           # Don't remove routes on bird shutdown
  scan time 2;       # Scan kernel routing table every 2 seconds
  graceful restart;  # Turn on graceful restart to reduce potential flaps in
                     # routes when reloading BIRD configuration.  With a full
                     # automatic mesh, there is no way to prevent BGP from
                     # flapping since multiple nodes update their BGP
                     # configuration at the same time, GR is not guaranteed to
                     # work correctly in this scenario.
  merge paths on;    # Allow export multipath routes (ECMP)
  metric 0;          # Preserve per-route krt_metric
}
# Configure synchronization between routing tables and kernel (IPv6).
protocol kernel {
  ipv6 {
    export filter calico_kernel_programming_v6; # Default is export none
    import all;
  };
  learn;             # Learn all alien routes from the kernel
  persist;           # Don't remove routes on bird shutdown
  scan time 2;       # Scan kernel routing table every 2 seconds
  graceful restart;
  merge paths on;    # Allow export multipath routes (ECMP)
  metric 0;          # Preserve per-route krt_metric
}
# Template for all IPv4 BGP clients
template bgp bgp_template_v4 {
  debug { states, routes, filters, events };
  description "Connection to BGP peer";
  local as 64512;
  connect delay time 2;
  connect retry time 5;
  error wait time 5,30;
  ipv4 {
    gateway recursive; # This should be the default, but just in case.
    add paths on;
    graceful restart;  # See comment in kernel section about graceful restart.
  };
}
# -------------- IPv4 BGP Filters ------------------
# v4 BGPFilter import-only-filter
function 'bgp_import-only-filter_importFilterV4'() {
  if ((net ~ 44.0.0.0/16)) then { accept; }
}
# IPv4 BGP Protocol Configurations
protocol bgp Local_Workload_192_168_162_134 from bgp_template_v4 {
  local as 65002;
  neighbor 192.168.162.134 as 64516;
  ttl security off;
  multihop;
  passive on;
  ipv4 {
    import filter {
      'bgp_import-only-filter_importFilterV4'();
    if (defined(bgp_local_pref)&&(bgp_local_pref > 2147482623)) then
      preference = 200;
    accept;
    };
    export filter {
      if (defined(source) && source = RTS_BGP && defined(bgp_local_pref)) then {
      krt_metric = 2147483647 - bgp_local_pref;
    } else {
      if (!defined(krt_metric)) then { krt_metric = 1024; }
      bgp_local_pref = 2147483647 - krt_metric;
    }
    calico_export_to_bgp_peers_v4(false);
    reject;
    };  # Only want to export routes for workloads.
  };
}
protocol bgp Local_Workload_192_168_162_136 from bgp_template_v4 {
  local as 65001;
  neighbor 192.168.162.136 as 64517;
  ttl security off;
  multihop;
  passive on;
  ipv4 {
    import filter {
      'bgp_import-only-filter_importFilterV4'();
    if (defined(bgp_local_pref)&&(bgp_local_pref > 2147482623)) then
      preference = 200;
    accept;
    };
    export filter {
      if (defined(source) && source = RTS_BGP && defined(bgp_local_pref)) then {
      krt_metric = 2147483647 - bgp_local_pref;
    } else {
      if (!defined(krt_metric)) then { krt_metric = 1024; }
      bgp_local_pref = 2147483647 - krt_metric;
    }
    calico_export_to_bgp_peers_v4(false);
    reject;
    };  # Only want to export routes for workloads.
  };
}
# Template for all IPv6 BGP clients
template bgp bgp_template_v6 {
  debug { states, routes, filters, events };
  description "Connection to BGP peer";
  local as 64512;
  connect delay time 2;
  connect retry time 5;
  error wait time 5,30;
  ipv6 {
    gateway recursive; # This should be the default, but just in case.
    add paths on;
    graceful restart;  # See comment in kernel section about graceful restart.
  };
}
# -------------- IPv6 BGP Filters ------------------
# v6 BGPFilter import-only-filter
function 'bgp_import-only-filter_importFilterV6'() {
  if ((net ~ 5000::0/64)) then { accept; }
}
# IPv6 BGP Protocol Configurations
protocol bgp Local_Workload_fd00_10_244_0_586d_4461_e980_a284 from bgp_template_v6 {
  local as 65002;
  neighbor fd00:10:244:0:586d:4461:e980:a284 as 64516;
  ttl security off;
  multihop;
  passive on;
  ipv6 {
    import filter {
      'bgp_import-only-filter_importFilterV6'();
    if (defined(bgp_local_pref)&&(bgp_local_pref > 2147482623)) then
      preference = 200;
    accept;
    };
    export filter {
      if (defined(source) && source = RTS_BGP && defined(bgp_local_pref)) then {
      krt_metric = 2147483647 - bgp_local_pref;
    } else {
      if (!defined(krt_metric)) then { krt_metric = 1024; }
      bgp_local_pref = 2147483647 - krt_metric;
    }
    calico_export_to_bgp_peers_v6(false);
    reject;
    };  # Only want to export routes for workloads.
  };
}
protocol bgp Local_Workload_fd00_10_244_0_586d_4461_e980_a286 from bgp_template_v6 {
  local as 65001;
  neighbor fd00:10:244:0:586d:4461:e980:a286 as 64517;
  ttl security off;
  multihop;
  passive on;
  ipv6 {
    import filter {
      'bgp_import-only-filter_importFilterV6'();
    if (defined(bgp_local_pref)&&(bgp_local_pref > 2147482623)) then
      preference = 200;
    accept;
    };
    export filter {
      if (defined(source) && source = RTS_BGP && defined(bgp_local_pref)) then {
      krt_metric = 2147483647 - bgp_local_pref;
    } else {
      if (!defined(krt_metric)) then { krt_metric = 1024; }
      bgp_local_pref = 2147483647 - krt_metric;
    }
    calico_export_to_bgp_peers_v6(false);
    reject;
    };  # Only want to export routes for workloads.
  };
}