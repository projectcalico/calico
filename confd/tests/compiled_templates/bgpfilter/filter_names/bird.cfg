function apply_communities ()
{
}

# Generated by confd
include "bird_aggr.cfg";
include "bird_ipam.cfg";

router id 10.192.0.2;


# Configure synchronization between routing tables and kernel.
protocol kernel {
  learn;             # Learn all alien routes from the kernel
  persist;           # Don't remove routes on bird shutdown
  scan time 2;       # Scan kernel routing table every 2 seconds
  import all;
  export filter calico_kernel_programming; # Default is export none
  graceful restart;  # Turn on graceful restart to reduce potential flaps in
                     # routes when reloading BIRD configuration.  With a full
                     # automatic mesh, there is no way to prevent BGP from
                     # flapping since multiple nodes update their BGP
                     # configuration at the same time, GR is not guaranteed to
                     # work correctly in this scenario.
  merge paths on;    # Allow export multipath routes (ECMP)
}

# Watch interface up/down events.
protocol device {
  debug { states };
  scan time 2;    # Scan interfaces every 2 seconds
}

protocol direct {
  debug { states };
  interface -"cali*", -"kube-ipvs*", "*"; # Exclude cali* and kube-ipvs* but
                                          # include everything else.  In
                                          # IPVS-mode, kube-proxy creates a
                                          # kube-ipvs0 interface. We exclude
                                          # kube-ipvs0 because this interface
                                          # gets an address for every in use
                                          # cluster IP. We use static routes
                                          # for when we legitimately want to
                                          # export cluster IPs.
}

protocol bfd {
}


# Template for all BGP clients
template bgp bgp_template {
  debug { states, routes, filters, events };
  description "Connection to BGP peer";
  local as 64512;
  add paths on;
  graceful restart;  # See comment in kernel section about graceful restart.
  connect delay time 2;
  connect retry time 5;
  error wait time 5,30;
}

# -------------- BGP Filters ------------------
# BGPFilter 43characters.exactly.so.should.not.truncate
function 'bgp_43characters.exactly.so.should.not.truncate_import_filter_v4'()
{
  if ( net ~ 44.0.0.0/16 ) then { accept; }
  if ( net ~ 44.1.0.0/16 ) then { reject; }
}
function 'bgp_43characters.exactly.so.should.not.truncate_export_filter_v4'()
{
  if ( net ~ 77.0.0.0/16 ) then { accept; }
  if ( net ~ 77.1.0.0/16 ) then { reject; }
}
# BGPFilter 44characters.exactly.so.should.truncate-1234
function 'bgp_44characters.exactly.so.sh_A792EC9FC647E4BA_import_filter_v4'()
{
  if ( net ~ 44.2.0.0/16 ) then { accept; }
  if ( net ~ 44.3.0.0/16 ) then { reject; }
}
function 'bgp_44characters.exactly.so.sh_A792EC9FC647E4BA_export_filter_v4'()
{
  if ( net ~ 77.2.0.0/16 ) then { accept; }
  if ( net ~ 77.3.0.0/16 ) then { reject; }
}


# ------------- Node-to-node mesh -------------

# Node-to-node mesh disabled



# ------------- Global peers -------------



# For peer /global/peer_v4/10.192.0.2
# Skipping ourselves (10.192.0.2)


# For peer /global/peer_v4/10.192.0.3
protocol bgp Global_10_192_0_3 from bgp_template {
  neighbor 10.192.0.3 as 64512;
  source address 10.192.0.2;  # The local address we use for the TCP connection
  ttl security off;
  multihop;
  import filter {
    'bgp_43characters.exactly.so.should.not.truncate_import_filter_v4'();
    'bgp_44characters.exactly.so.sh_A792EC9FC647E4BA_import_filter_v4'();
    accept; # Prior to introduction of BGP Filters we used "import all" so use default accept behaviour on import
  };
  export filter {
    'bgp_43characters.exactly.so.should.not.truncate_export_filter_v4'();
    'bgp_44characters.exactly.so.sh_A792EC9FC647E4BA_export_filter_v4'();
    calico_export_to_bgp_peers();
    reject; # Prior to introduction of BGP Filters anything not explicitly exported through calico_export_to_bgp_peers()
            # was rejected so use default reject behaviour on export
  };  # Only want to export routes for workloads.
  gateway recursive;
}


# For peer /global/peer_v4/10.192.0.4
protocol bgp Global_10_192_0_4 from bgp_template {
  neighbor 10.192.0.4 as 64512;
  source address 10.192.0.2;  # The local address we use for the TCP connection
  ttl security off;
  multihop;
  import filter {
    'bgp_43characters.exactly.so.should.not.truncate_import_filter_v4'();
    'bgp_44characters.exactly.so.sh_A792EC9FC647E4BA_import_filter_v4'();
    accept; # Prior to introduction of BGP Filters we used "import all" so use default accept behaviour on import
  };
  export filter {
    'bgp_43characters.exactly.so.should.not.truncate_export_filter_v4'();
    'bgp_44characters.exactly.so.sh_A792EC9FC647E4BA_export_filter_v4'();
    calico_export_to_bgp_peers();
    reject; # Prior to introduction of BGP Filters anything not explicitly exported through calico_export_to_bgp_peers()
            # was rejected so use default reject behaviour on export
  };  # Only want to export routes for workloads.
  gateway recursive;
}




# ------------- Node-specific peers -------------

# No node-specific peers configured.

