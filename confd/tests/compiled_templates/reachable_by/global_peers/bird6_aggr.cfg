# Generated by confd

protocol static {
   # Static routes needed for global BGP peers.
   route ffee::10/128 via ffee::1:1;
   route ffee::11/128 via ffee::1:1;
}


# Aggregation of routes on this host; export the block, nothing beneath it.
function calico_aggr ()
{
}
