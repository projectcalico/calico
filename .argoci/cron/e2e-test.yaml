# Generated by hack/cmd/semaphore2argo from a Semaphore e2e pipeline. DO NOT EDIT.
# Condensed ArgoCI workflow; cc-argoci-handler expands it. See .argoci/DESIGN.md.
version: condensed
generateName: e2e-test-
schedule: 0 0 31 2 *
globalPrologue: |
    export FUNCTIONAL_AREA="${FUNCTIONAL_AREA:-test.yml}"
    export K8S_E2E_FLAGS="${K8S_E2E_FLAGS:---ginkgo.focus=(\[sig-calico\].*\[Conformance\]) --ginkgo.skip=(\[Slow\]|\[Disruptive\]|\[DataPath\]|NoTierPrefix)}"
    export PRIVATE_REGISTRY="${PRIVATE_REGISTRY:-quay.io/}"
    export PRODUCT="${PRODUCT:-calico}"
    export TEST_TYPE="${TEST_TYPE:-k8s-e2e}"
    export USE_PRIVATE_REGISTRY="${USE_PRIVATE_REGISTRY:-true}"
    source .argoci/scripts/global_prologue.sh
globalEpilogue: |
    source .argoci/scripts/global_epilogue.sh
defaults:
    secretBundles:
        - banzai-secrets
steps:
    - name: calico-tests
      services:
        - docker
      matrix:
        - name: gcp-kubeadm-stable-operator-calicobpf
          env:
            - name: PROVISIONER
              value: gcp-kubeadm
            - name: K8S_VERSION
              value: stable
            - name: INSTALLER
              value: operator
            - name: DATAPLANE
              value: CalicoBPF
      commands: |
        .argoci/scripts/body_standard.sh
