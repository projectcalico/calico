# The upstream Gateway API conformance suite, run against Calico with Envoy
# Gateway on a kind cluster.
version: condensed-module

defaults:
  timeout: 1h
  services:
    - docker

steps:
  - name: gateway-conformance
    requests: {cpu: 5000m, memory: 16Gi}
    depends:
      - build-calico-image
      - build-node-image
      - build-whisker-image
      - build-operator-image
      - build-third-party-images
    commands: |
      echo "$DOCKERHUB_PASSWORD" | docker login --username "$DOCKERHUB_USERNAME" --password-stdin
      export EXTRA_DOCKER_ARGS="-e GOPROXY=${GOPROXY}"
      # Marker files come with them, so make treats the images as already built.
      .argoci/scripts/load-cached-images calico node whisker
      make e2e-test-gateway-conformance
    epilogue: |
      cd "${CI_HOME}/${CI_GIT_DIR}"
      artifact push job ./report/ --destination gateway-conformance-reports || true
