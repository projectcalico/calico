# Felix's own checks, and the FV fleet.
#
# The FVs need a real kernel per dataplane and per Ubuntu release, so each lane
# drives a fleet of GCE VMs from a pod — it compiles nothing and spends its hour
# on ssh.
version: condensed-module

defaults:
  timeout: 1h
  env:
    # A cold build would re-clone libbpf, re-exposing the github.com/libbpf HTTP
    # 500 flake. The tarball the calico-image producer ships is the only source
    # any of these steps should need.
    - name: NO_LIBBPF_CLONE
      value: "true"

steps:
  # Everything a test VM unpacks comes from here, so a lane whose batches all
  # fail to configure usually failed further up.
  - name: felix-build
    depends: [build-calico-image, check-go]
    timeout: 40m
    requests: {cpu: 4000m, memory: 8Gi}
    services:
      - docker
    secretBundles:
      - name: google-service-account-for-gce
        keys:
          - name: google-service-account-for-gce-key.json
            as: GCE_SERVICE_ACCOUNT_KEY
    commands: |
      echo "$DOCKERHUB_PASSWORD" | docker login --username "$DOCKERHUB_USERNAME" --password-stdin
      export EXTRA_DOCKER_ARGS="-e GOPROXY=${GOPROXY}"
      source .argoci/scripts/vm-fleet-setup.sh
      .argoci/scripts/load-cached-images calico
      .semaphore/scripts/load-felix-prereqs.sh
      cd felix
      # REMOTE_DEPS= keeps this from rebuilding the image and race binary already
      # shipped. `make build` is left out because on amd64 it also cross-compiles
      # the Windows binary, which nothing in the Linux FV path consumes.
      make REMOTE_DEPS= bin/calico-felix image ut-bpf-prereqs fv-prereqs
      ./.semaphore/cache-test-artifacts

  - name: felix-static-checks
    depends: [build-calico-image, check-go]
    timeout: 30m
    requests: {cpu: 4000m, memory: 6Gi}
    services:
      - docker
    commands: |
      echo "$DOCKERHUB_PASSWORD" | docker login --username "$DOCKERHUB_USERNAME" --password-stdin
      export EXTRA_DOCKER_ARGS="-e GOPROXY=${GOPROXY}"
      .argoci/scripts/load-cached-images calico
      .semaphore/scripts/load-felix-prereqs.sh
      cd felix
      make static-checks

  # Produces nothing the FV fleet consumes, so it runs alongside rather than
  # ahead of it.
  #
  # Unprivileged because two of its suites read geteuid(): one asserts a write to
  # a root-owned path is refused, and the BPF wrapper picks its real or mock
  # implementation from it. As root both take the wrong branch.
  - name: felix-ut
    depends: [build-calico-image, check-go]
    timeout: 45m
    runAsNonRoot: true
    requests: {cpu: 4000m, memory: 6Gi}
    services:
      - docker
    commands: |
      echo "$DOCKERHUB_PASSWORD" | docker login --username "$DOCKERHUB_USERNAME" --password-stdin
      export EXTRA_DOCKER_ARGS="-e GOPROXY=${GOPROXY}"
      .argoci/scripts/load-cached-images calico
      .semaphore/scripts/load-felix-prereqs.sh
      cd felix
      make ut
    epilogue: |
      cd "${CI_HOME}/${CI_GIT_DIR}/felix"
      artifact push job report --destination test-results || true

  # On a machine because the suite loads ip6_tables, and a pod cannot insert a
  # module at all — neither the tools nor the privileges are there. It also runs
  # the combined binary as `component typha`, hence the preloaded image.
  - name: felix-k8sfv
    depends: [build-calico-image, check-go]
    timeout: 45m
    runOn: vm
    vm:
      machineType: e2-standard-4
    services:
      - docker
    commands: |
      sudo apt-get update -y && sudo apt-get install -y make
      echo "$DOCKERHUB_PASSWORD" | docker login --username "$DOCKERHUB_USERNAME" --password-stdin
      export EXTRA_DOCKER_ARGS="-e GOPROXY=${GOPROXY}"
      .argoci/scripts/load-cached-images calico
      .semaphore/scripts/load-felix-prereqs.sh
      cd felix
      make REMOTE_DEPS= k8sfv-test JUST_A_MINUTE=true USE_TYPHA=true
      make REMOTE_DEPS= k8sfv-test JUST_A_MINUTE=true USE_TYPHA=false
    epilogue: |
      copyFromVirtualMachine felix/report report || true
      artifact push job report --destination test-results || true

  # FELIX_TEST_GROUP is parsed, not just labelled: the Ubuntu release, dataplane
  # and whether UTs come along are all read back out of this one string.
  #
  # The step deadline deliberately exceeds JOB_TIMEOUT_MINUTES. That is the
  # fleet's watchdog, which captures a hung VM's serial console before killing
  # the batch; a step killed first would take the diagnostics with it.
  - name: felix-fv
    depends: [felix-build]
    timeout: 1h15m
    requests: {cpu: 2000m, memory: 4Gi}
    secretBundles:
      - name: google-service-account-for-gce
        keys:
          - name: google-service-account-for-gce-key.json
            as: GCE_SERVICE_ACCOUNT_KEY
    env:
      - name: JOB_TIMEOUT_MINUTES
        value: "60"
    matrix:
      - name: iptables-2204
        env:
          - name: FELIX_TEST_GROUP
            value: 22.04-ipt
          - name: FV_BATCHES_OVERRIDE
            value: "16"
      - name: nftables-2204
        env:
          - name: FELIX_TEST_GROUP
            value: 22.04-nft
          - name: FV_BATCHES_OVERRIDE
            value: "16"
      - name: bpf-2404-iptables
        env:
          - name: FELIX_TEST_GROUP
            value: bpf-24.04-ipt-with-ut
          - name: FV_BATCHES_OVERRIDE
            value: "60"
      - name: bpf-2204-nftables
        env:
          - name: FELIX_TEST_GROUP
            value: bpf-22.04-nft-with-ut
          - name: FELIX_FV_BPFATTACHTYPE
            value: tc
          # Conntrack only, to keep the lane inside its hour.
          - name: FV_FOCUS_OVERRIDE
            value: _BPF_.*ct=true
          - name: FV_BATCHES_OVERRIDE
            value: "10"
      - name: bpf-2604-netkit
        env:
          - name: FELIX_TEST_GROUP
            value: bpf-26.04-nft-no-fv-with-ut
          # No FELIX_FV_BPFATTACHTYPE: this lane covers netkit attachment, which
          # the default selects for netkit workload devices. Naming one
          # mechanism would opt the node out of netkit entirely.
          - name: FELIX_FV_NETKIT
            value: Enabled
          - name: FV_BATCHES_OVERRIDE
            value: "60"
      - name: bpf-2604-jitharden
        env:
          - name: FELIX_TEST_GROUP
            value: bpf-26.04-nft-with-ut-jitharden
          - name: FELIX_FV_BPFATTACHTYPE
            value: tc
          # TCP and conntrack only, to keep the lane inside its hour.
          - name: FV_FOCUS_OVERRIDE
            value: _BPF_.*tcp.*ct=true
          - name: FV_BATCHES_OVERRIDE
            value: "16"
    commands: |
      source .argoci/scripts/vm-fleet-setup.sh
      source felix/.semaphore/fv-prologue
      # After the prologue, which derives its own values for both from
      # FELIX_TEST_GROUP and would otherwise win.
      if [ -n "${FV_BATCHES_OVERRIDE:-}" ]; then
        export NUM_FV_BATCHES="${FV_BATCHES_OVERRIDE}"
      fi
      if [ -n "${FV_FOCUS_OVERRIDE:-}" ]; then
        export FV_FOCUS="${FV_FOCUS_OVERRIDE}"
      fi
      .semaphore/vms/run-tests-on-vms "${VM_PREFIX}"
    epilogue: |
      # The commands ran in a subshell, so nothing they derived survives here —
      # VM_PREFIX above all, without which the teardown reaches no fleet.
      cd "${CI_HOME}/${CI_GIT_DIR}"
      source .argoci/scripts/vm-fleet-setup.sh
      source felix/.semaphore/fv-prologue
      source felix/.semaphore/fv-epilogue

  # Separate from the Linux prerequisites because nothing in that path consumes
  # these.
  - name: felix-build-windows
    depends: [check-go]
    requests: {cpu: 4250m, memory: 5.5Gi}
    services:
      - docker
    commands: |
      echo "$DOCKERHUB_PASSWORD" | docker login --username "$DOCKERHUB_USERNAME" --password-stdin
      export EXTRA_DOCKER_ARGS="-e GOPROXY=${GOPROXY}"
      cd felix
      make bin/calico-felix.exe fv/win-fv.exe

  # A kubeadm cluster of Azure VMs driven by Azure Service Operator, installed
  # from the images this commit built rather than a published hashrelease.
  - name: felix-windows-fv
    depends: [felix-build-windows, build-calico-image, build-node-image]
    timeout: 1h30m
    requests: {cpu: 3000m, memory: 6Gi}
    services:
      - docker
    secretBundles:
      - banzai-secrets
    env:
      - name: FV_TYPE
        value: calico-felix
      - name: RELEASE_STREAM
        value: local-build
      - name: HASH_RELEASE
        value: "false"
      - name: CONTAINERD_VERSION
        value: 1.7.28
      # Where the FV script writes, which it hardcodes rather than reading from
      # here. Naming anything else loses the logs that explain a failure.
      - name: LOGS_DIR
        value: /home/semaphore/fv.log
    commands: |
      export AZURE_SUBSCRIPTION_ID="$AZ_SUBSCRIPTION_ID"
      export AZURE_TENANT_ID="$AZ_TENANT_ID"
      export AZURE_CLIENT_ID="$AZ_SP_ID"
      export AZURE_CLIENT_SECRET="$AZ_SP_PASSWORD"
      export AZURE_RESOURCE_GROUP="${ARGOCI_RESOURCE_PREFIX}-win-felix-${CI_GIT_SHA:0:4}-pr${CI_GIT_PR_NUMBER:-merge}-rg"
      export REPORT_DIR="${CI_HOME}/${CI_GIT_DIR}/process/testing/winfv-felix/report"
      az login --service-principal -u "$AZ_SP_ID" -p "$AZ_SP_PASSWORD" --tenant "$AZ_TENANT_ID" --output none
      echo "$DOCKERHUB_PASSWORD" | docker login --username "$DOCKERHUB_USERNAME" --password-stdin
      .argoci/scripts/load-cached-images calico node
      ./process/testing/winfv-felix/run-win-fv.sh
    epilogue: |
      cd "${CI_HOME}/${CI_GIT_DIR}"
      # Skipped specs are reported as failures otherwise.
      python3 hack/strip-skipped-tests.py process/testing/winfv-felix/report/*.xml 2>/dev/null || true
      artifact push job process/testing/winfv-felix/report --destination test-results || true
      artifact push job "${LOGS_DIR}" --destination logs || true
      # Azure resources outlive the step, so this logs in again rather than
      # relying on the session the commands opened.
      export AZURE_SUBSCRIPTION_ID="$AZ_SUBSCRIPTION_ID"
      export AZURE_TENANT_ID="$AZ_TENANT_ID"
      export AZURE_CLIENT_ID="$AZ_SP_ID"
      export AZURE_CLIENT_SECRET="$AZ_SP_PASSWORD"
      export AZURE_RESOURCE_GROUP="${ARGOCI_RESOURCE_PREFIX}-win-felix-${CI_GIT_SHA:0:4}-pr${CI_GIT_PR_NUMBER:-merge}-rg"
      az login --service-principal -u "$AZ_SP_ID" -p "$AZ_SP_PASSWORD" --tenant "$AZ_TENANT_ID" --output none || true
      cd process/testing/aso && make dist-clean || true
