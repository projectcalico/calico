apiVersion: v1
kind: PersistentVolume
metadata:
  name: tigera-elasticsearch
spec:
  capacity:
    storage: 10Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: /mnt/tigera/elastic-data
  persistentVolumeReclaimPolicy: Recycle
  storageClassName: tigera-elasticsearch
