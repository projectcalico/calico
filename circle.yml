general:
  artifacts:
    - "release"
machine:
  services:
    - docker
test:
  override:
    - make release/calicoctl test-containerized
experimental:
  notify:
    branches:
      only:
        - master
