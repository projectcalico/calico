general:
  artifacts:
    - "dist"
machine:
  services:
    - docker
dependencies:
  override:
    - sudo pip install -r build_calicoctl/requirements.txt
test:
  override:
    - docker run --net=host -d  quay.io/coreos/etcd:v2.0.8 && nosetests -c nose.cfg --cover-html-dir=$CIRCLE_ARTIFACTS
    - cd build_calicoctl && ./create_binary.sh
