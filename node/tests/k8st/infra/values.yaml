installation:
  enabled: true
  kubernetesProvider: ""
  registry: docker.io
  controlPlaneReplicas: 1
  calicoNetwork:
    ipPools:
    - cidr: 192.168.0.0/16
    - cidr: fd00:10:244::/64

apiServer:
  enabled: true

goldmane:
  enabled: false

whisker:
  enabled: false

# Configuration for the tigera operator
tigeraOperator:
  image: tigera/operator
  version: test-build
  registry: docker.io
