apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: bgpfilters.projectcalico.org
spec:
  group: projectcalico.org
  names:
    kind: BGPFilter
    listKind: BGPFilterList
    plural: bgpfilters
    singular: bgpfilter
  preserveUnknownFields: false
  scope: Cluster
  versions:
    - name: v3
      schema:
        openAPIV3Schema:
          properties:
            apiVersion:
              type: string
            kind:
              type: string
            metadata:
              type: object
            spec:
              properties:
                exportV4:
                  items:
                    properties:
                      action:
                        enum:
                          - Accept
                          - Reject
                        type: string
                      asPathPrefix:
                        items:
                          format: int32
                          type: integer
                        type: array
                      cidr:
                        format: cidr
                        type: string
                      communities:
                        properties:
                          values:
                            items:
                              type: string
                            maxItems: 1
                            minItems: 1
                            type: array
                            x-kubernetes-validations:
                              - message:
                                  standard communities must have 16-bit values
                                  (aa:nn), large communities must have 32-bit values (aa:nn:mm)
                                rule:
                                  "self.all(v, v.matches('^[0-9]+:[0-9]+$') ? (int(v.split(':')[0])
                                  <= 65535 && int(v.split(':')[1]) <= 65535) : (v.matches('^[0-9]+:[0-9]+:[0-9]+$')
                                  && int(v.split(':')[0]) <= 4294967295 && int(v.split(':')[1])
                                  <= 4294967295 && int(v.split(':')[2]) <= 4294967295))"
                        required:
                          - values
                        type: object
                        x-kubernetes-map-type: atomic
                      interface:
                        type: string
                      matchOperator:
                        enum:
                          - Equal
                          - NotEqual
                          - In
                          - NotIn
                        type: string
                      operations:
                        items:
                          properties:
                            addCommunity:
                              properties:
                                value:
                                  type: string
                                  x-kubernetes-validations:
                                    - message:
                                        standard communities must have 16-bit values
                                        (aa:nn), large communities must have 32-bit values
                                        (aa:nn:mm)
                                      rule:
                                        "self.matches('^[0-9]+:[0-9]+$') ? (int(self.split(':')[0])
                                        <= 65535 && int(self.split(':')[1]) <= 65535)
                                        : (self.matches('^[0-9]+:[0-9]+:[0-9]+$') &&
                                        int(self.split(':')[0]) <= 4294967295 && int(self.split(':')[1])
                                        <= 4294967295 && int(self.split(':')[2]) <=
                                        4294967295)"
                              required:
                                - value
                              type: object
                              x-kubernetes-map-type: atomic
                            prependASPath:
                              properties:
                                prefix:
                                  items:
                                    format: int32
                                    type: integer
                                  maxItems: 10
                                  minItems: 1
                                  type: array
                              required:
                                - prefix
                              type: object
                              x-kubernetes-map-type: atomic
                            setPriority:
                              properties:
                                value:
                                  maximum: 2147483646
                                  minimum: 1
                                  type: integer
                              required:
                                - value
                              type: object
                              x-kubernetes-map-type: atomic
                          type: object
                          x-kubernetes-map-type: atomic
                          x-kubernetes-validations:
                            - message: exactly one operation must be set
                              rule:
                                "(has(self.addCommunity) ? 1 : 0) + (has(self.prependASPath)
                                ? 1 : 0) + (has(self.setPriority) ? 1 : 0) == 1"
                        maxItems: 10
                        minItems: 1
                        type: array
                      peerType:
                        enum:
                          - eBGP
                          - iBGP
                        type: string
                      prefixLength:
                        properties:
                          max:
                            format: int32
                            maximum: 32
                            minimum: 0
                            type: integer
                          min:
                            format: int32
                            maximum: 32
                            minimum: 0
                            type: integer
                        type: object
                        x-kubernetes-map-type: atomic
                      priority:
                        maximum: 2147483646
                        minimum: 1
                        type: integer
                      source:
                        enum:
                          - RemotePeers
                        type: string
                    required:
                      - action
                    type: object
                    x-kubernetes-map-type: atomic
                    x-kubernetes-validations:
                      - message: operations may only be used with action Accept
                        rule:
                          "!has(self.operations) || size(self.operations) == 0 ||
                          self.action == 'Accept'"
                  type: array
                exportV6:
                  items:
                    properties:
                      action:
                        enum:
                          - Accept
                          - Reject
                        type: string
                      asPathPrefix:
                        items:
                          format: int32
                          type: integer
                        type: array
                      cidr:
                        format: cidr
                        type: string
                      communities:
                        properties:
                          values:
                            items:
                              type: string
                            maxItems: 1
                            minItems: 1
                            type: array
                            x-kubernetes-validations:
                              - message:
                                  standard communities must have 16-bit values
                                  (aa:nn), large communities must have 32-bit values (aa:nn:mm)
                                rule:
                                  "self.all(v, v.matches('^[0-9]+:[0-9]+$') ? (int(v.split(':')[0])
                                  <= 65535 && int(v.split(':')[1]) <= 65535) : (v.matches('^[0-9]+:[0-9]+:[0-9]+$')
                                  && int(v.split(':')[0]) <= 4294967295 && int(v.split(':')[1])
                                  <= 4294967295 && int(v.split(':')[2]) <= 4294967295))"
                        required:
                          - values
                        type: object
                        x-kubernetes-map-type: atomic
                      interface:
                        type: string
                      matchOperator:
                        enum:
                          - Equal
                          - NotEqual
                          - In
                          - NotIn
                        type: string
                      operations:
                        items:
                          properties:
                            addCommunity:
                              properties:
                                value:
                                  type: string
                                  x-kubernetes-validations:
                                    - message:
                                        standard communities must have 16-bit values
                                        (aa:nn), large communities must have 32-bit values
                                        (aa:nn:mm)
                                      rule:
                                        "self.matches('^[0-9]+:[0-9]+$') ? (int(self.split(':')[0])
                                        <= 65535 && int(self.split(':')[1]) <= 65535)
                                        : (self.matches('^[0-9]+:[0-9]+:[0-9]+$') &&
                                        int(self.split(':')[0]) <= 4294967295 && int(self.split(':')[1])
                                        <= 4294967295 && int(self.split(':')[2]) <=
                                        4294967295)"
                              required:
                                - value
                              type: object
                              x-kubernetes-map-type: atomic
                            prependASPath:
                              properties:
                                prefix:
                                  items:
                                    format: int32
                                    type: integer
                                  maxItems: 10
                                  minItems: 1
                                  type: array
                              required:
                                - prefix
                              type: object
                              x-kubernetes-map-type: atomic
                            setPriority:
                              properties:
                                value:
                                  maximum: 2147483646
                                  minimum: 1
                                  type: integer
                              required:
                                - value
                              type: object
                              x-kubernetes-map-type: atomic
                          type: object
                          x-kubernetes-map-type: atomic
                          x-kubernetes-validations:
                            - message: exactly one operation must be set
                              rule:
                                "(has(self.addCommunity) ? 1 : 0) + (has(self.prependASPath)
                                ? 1 : 0) + (has(self.setPriority) ? 1 : 0) == 1"
                        maxItems: 10
                        minItems: 1
                        type: array
                      peerType:
                        enum:
                          - eBGP
                          - iBGP
                        type: string
                      prefixLength:
                        properties:
                          max:
                            format: int32
                            maximum: 128
                            minimum: 0
                            type: integer
                          min:
                            format: int32
                            maximum: 128
                            minimum: 0
                            type: integer
                        type: object
                        x-kubernetes-map-type: atomic
                      priority:
                        maximum: 2147483646
                        minimum: 1
                        type: integer
                      source:
                        enum:
                          - RemotePeers
                        type: string
                    required:
                      - action
                    type: object
                    x-kubernetes-map-type: atomic
                    x-kubernetes-validations:
                      - message: operations may only be used with action Accept
                        rule:
                          "!has(self.operations) || size(self.operations) == 0 ||
                          self.action == 'Accept'"
                  type: array
                importV4:
                  items:
                    properties:
                      action:
                        enum:
                          - Accept
                          - Reject
                        type: string
                      asPathPrefix:
                        items:
                          format: int32
                          type: integer
                        type: array
                      cidr:
                        format: cidr
                        type: string
                      communities:
                        properties:
                          values:
                            items:
                              type: string
                            maxItems: 1
                            minItems: 1
                            type: array
                            x-kubernetes-validations:
                              - message:
                                  standard communities must have 16-bit values
                                  (aa:nn), large communities must have 32-bit values (aa:nn:mm)
                                rule:
                                  "self.all(v, v.matches('^[0-9]+:[0-9]+$') ? (int(v.split(':')[0])
                                  <= 65535 && int(v.split(':')[1]) <= 65535) : (v.matches('^[0-9]+:[0-9]+:[0-9]+$')
                                  && int(v.split(':')[0]) <= 4294967295 && int(v.split(':')[1])
                                  <= 4294967295 && int(v.split(':')[2]) <= 4294967295))"
                        required:
                          - values
                        type: object
                        x-kubernetes-map-type: atomic
                      interface:
                        type: string
                      matchOperator:
                        enum:
                          - Equal
                          - NotEqual
                          - In
                          - NotIn
                        type: string
                      operations:
                        items:
                          properties:
                            addCommunity:
                              properties:
                                value:
                                  type: string
                                  x-kubernetes-validations:
                                    - message:
                                        standard communities must have 16-bit values
                                        (aa:nn), large communities must have 32-bit values
                                        (aa:nn:mm)
                                      rule:
                                        "self.matches('^[0-9]+:[0-9]+$') ? (int(self.split(':')[0])
                                        <= 65535 && int(self.split(':')[1]) <= 65535)
                                        : (self.matches('^[0-9]+:[0-9]+:[0-9]+$') &&
                                        int(self.split(':')[0]) <= 4294967295 && int(self.split(':')[1])
                                        <= 4294967295 && int(self.split(':')[2]) <=
                                        4294967295)"
                              required:
                                - value
                              type: object
                              x-kubernetes-map-type: atomic
                            prependASPath:
                              properties:
                                prefix:
                                  items:
                                    format: int32
                                    type: integer
                                  maxItems: 10
                                  minItems: 1
                                  type: array
                              required:
                                - prefix
                              type: object
                              x-kubernetes-map-type: atomic
                            setPriority:
                              properties:
                                value:
                                  maximum: 2147483646
                                  minimum: 1
                                  type: integer
                              required:
                                - value
                              type: object
                              x-kubernetes-map-type: atomic
                          type: object
                          x-kubernetes-map-type: atomic
                          x-kubernetes-validations:
                            - message: exactly one operation must be set
                              rule:
                                "(has(self.addCommunity) ? 1 : 0) + (has(self.prependASPath)
                                ? 1 : 0) + (has(self.setPriority) ? 1 : 0) == 1"
                        maxItems: 10
                        minItems: 1
                        type: array
                      peerType:
                        enum:
                          - eBGP
                          - iBGP
                        type: string
                      prefixLength:
                        properties:
                          max:
                            format: int32
                            maximum: 32
                            minimum: 0
                            type: integer
                          min:
                            format: int32
                            maximum: 32
                            minimum: 0
                            type: integer
                        type: object
                        x-kubernetes-map-type: atomic
                      priority:
                        maximum: 2147483646
                        minimum: 1
                        type: integer
                      source:
                        enum:
                          - RemotePeers
                        type: string
                    required:
                      - action
                    type: object
                    x-kubernetes-map-type: atomic
                    x-kubernetes-validations:
                      - message: operations may only be used with action Accept
                        rule:
                          "!has(self.operations) || size(self.operations) == 0 ||
                          self.action == 'Accept'"
                  type: array
                  x-kubernetes-validations:
                    - message: source is not applicable to import rules
                      rule: self.all(r, !has(r.source) || r.source == '')
                importV6:
                  items:
                    properties:
                      action:
                        enum:
                          - Accept
                          - Reject
                        type: string
                      asPathPrefix:
                        items:
                          format: int32
                          type: integer
                        type: array
                      cidr:
                        format: cidr
                        type: string
                      communities:
                        properties:
                          values:
                            items:
                              type: string
                            maxItems: 1
                            minItems: 1
                            type: array
                            x-kubernetes-validations:
                              - message:
                                  standard communities must have 16-bit values
                                  (aa:nn), large communities must have 32-bit values (aa:nn:mm)
                                rule:
                                  "self.all(v, v.matches('^[0-9]+:[0-9]+$') ? (int(v.split(':')[0])
                                  <= 65535 && int(v.split(':')[1]) <= 65535) : (v.matches('^[0-9]+:[0-9]+:[0-9]+$')
                                  && int(v.split(':')[0]) <= 4294967295 && int(v.split(':')[1])
                                  <= 4294967295 && int(v.split(':')[2]) <= 4294967295))"
                        required:
                          - values
                        type: object
                        x-kubernetes-map-type: atomic
                      interface:
                        type: string
                      matchOperator:
                        enum:
                          - Equal
                          - NotEqual
                          - In
                          - NotIn
                        type: string
                      operations:
                        items:
                          properties:
                            addCommunity:
                              properties:
                                value:
                                  type: string
                                  x-kubernetes-validations:
                                    - message:
                                        standard communities must have 16-bit values
                                        (aa:nn), large communities must have 32-bit values
                                        (aa:nn:mm)
                                      rule:
                                        "self.matches('^[0-9]+:[0-9]+$') ? (int(self.split(':')[0])
                                        <= 65535 && int(self.split(':')[1]) <= 65535)
                                        : (self.matches('^[0-9]+:[0-9]+:[0-9]+$') &&
                                        int(self.split(':')[0]) <= 4294967295 && int(self.split(':')[1])
                                        <= 4294967295 && int(self.split(':')[2]) <=
                                        4294967295)"
                              required:
                                - value
                              type: object
                              x-kubernetes-map-type: atomic
                            prependASPath:
                              properties:
                                prefix:
                                  items:
                                    format: int32
                                    type: integer
                                  maxItems: 10
                                  minItems: 1
                                  type: array
                              required:
                                - prefix
                              type: object
                              x-kubernetes-map-type: atomic
                            setPriority:
                              properties:
                                value:
                                  maximum: 2147483646
                                  minimum: 1
                                  type: integer
                              required:
                                - value
                              type: object
                              x-kubernetes-map-type: atomic
                          type: object
                          x-kubernetes-map-type: atomic
                          x-kubernetes-validations:
                            - message: exactly one operation must be set
                              rule:
                                "(has(self.addCommunity) ? 1 : 0) + (has(self.prependASPath)
                                ? 1 : 0) + (has(self.setPriority) ? 1 : 0) == 1"
                        maxItems: 10
                        minItems: 1
                        type: array
                      peerType:
                        enum:
                          - eBGP
                          - iBGP
                        type: string
                      prefixLength:
                        properties:
                          max:
                            format: int32
                            maximum: 128
                            minimum: 0
                            type: integer
                          min:
                            format: int32
                            maximum: 128
                            minimum: 0
                            type: integer
                        type: object
                        x-kubernetes-map-type: atomic
                      priority:
                        maximum: 2147483646
                        minimum: 1
                        type: integer
                      source:
                        enum:
                          - RemotePeers
                        type: string
                    required:
                      - action
                    type: object
                    x-kubernetes-map-type: atomic
                    x-kubernetes-validations:
                      - message: operations may only be used with action Accept
                        rule:
                          "!has(self.operations) || size(self.operations) == 0 ||
                          self.action == 'Accept'"
                  type: array
                  x-kubernetes-validations:
                    - message: source is not applicable to import rules
                      rule: self.all(r, !has(r.source) || r.source == '')
              type: object
          required:
            - metadata
            - spec
          type: object
      served: true
      storage: true
