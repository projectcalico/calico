apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: (devel)
  creationTimestamp: null
  name: bgpfilters.crd.projectcalico.org
spec:
  group: crd.projectcalico.org
  names:
    kind: BGPFilter
    listKind: BGPFilterList
    plural: bgpfilters
    singular: bgpfilter
  scope: Cluster
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: BGPFilterSpec contains the filter rules of the BGP Filter.
            properties:
              export:
                description: The ordered set of BGPFilter rules acting on exporting
                  routes to a peer.
                items:
                  description: BGPFilterRule defines BGP filter rule consisting a
                    single IP CIDR block and an filter action on this CIDR.
                  properties:
                    action:
                      type: string
                    cidr:
                      type: string
                  required:
                  - action
                  type: object
                type: array
              import:
                description: The ordered set of BGPFilter rules acting on importing
                  routes from a peer.
                items:
                  description: BGPFilterRule defines BGP filter rule consisting a
                    single IP CIDR block and an filter action on this CIDR.
                  properties:
                    action:
                      type: string
                    cidr:
                      type: string
                  required:
                  - action
                  type: object
                type: array
            type: object
        type: object
    served: true
    storage: true
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
